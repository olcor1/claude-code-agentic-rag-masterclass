---
phase: 04-navigation-tools
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/app/services/explorer_agent.py
  - backend/app/services/chat.py
  - backend/tests/test_prd_smoke.py
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
must_haves:
  truths:
    - "The explorer tool registry exposes `ls` and `tree` as the filesystem-style navigation entry points."
    - "A filesystem-style chat request routes through the existing explorer path instead of bypassing navigation."
    - "`ls` and `tree` descriptions stay concise, root-aware, and compatible with later grep/glob/read phases."
  artifacts:
    - path: backend/app/services/explorer_agent.py
      provides: "`EXPLORER_TOOLS` contracts and dispatch for `ls`/`tree`"
    - path: backend/app/services/chat.py
      provides: "Chat-side explorer routing for filesystem-style questions"
    - path: backend/tests/test_prd_smoke.py
      provides: "Integration regressions for explorer-tool registration and chat routing"
  key_links:
    - from: backend/app/services/explorer_agent.py
      to: backend/app/services/knowledge_base.py
      via: "`execute_ls()`/`execute_tree()` dispatch"
      pattern: "execute_(ls|tree)"
    - from: backend/app/services/chat.py
      to: backend/app/services/explorer_agent.py
      via: "`looks_like_explorer_request()` -> `run_explorer_sub_agent()`"
      pattern: "run_explorer_sub_agent"
---

<objective>
Align the existing explorer and chat integration around the Phase 4 `ls`/`tree` contract without reworking later-phase tools.

Purpose: Keep the already-shipped explorer stack compatible with the hardened navigation service so the agent can actually consume TOOL-01 and TOOL-02 through its real integration path.
Output: Tightened `ls`/`tree` tool definitions, stable explorer dispatch, and chat-routing regression coverage that protects filesystem-style requests.
</objective>

<execution_context>
@C:/Users/olivi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/olivi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-navigation-tools/04-CONTEXT.md
@.planning/phases/04-navigation-tools/04-RESEARCH.md
@.planning/phases/04-navigation-tools/04-VALIDATION.md
@backend/app/services/knowledge_base.py
@backend/app/services/explorer_agent.py
@backend/app/services/chat.py
@backend/tests/test_prd_smoke.py

<interfaces>
From `backend/app/services/explorer_agent.py`:
```python
EXPLORER_TOOLS = [
    {"type": "function", "function": {"name": "ls", ...}},
    {"type": "function", "function": {"name": "tree", ...}},
    {"type": "function", "function": {"name": "grep", ...}},
    {"type": "function", "function": {"name": "glob", ...}},
    {"type": "function", "function": {"name": "read", ...}},
]

def run_explorer_sub_agent(
    db: Session,
    *,
    user_id: UUID | str,
    question: str,
    redaction_session: ConversationRedactionSession | None = None,
) -> ExplorerResult: ...
```

Current dispatch points:
```python
if tool_name == "ls":
    output = execute_ls(db, user_id, execution_arguments.get("path"))
elif tool_name == "tree":
    output = execute_tree(db, user_id, execution_arguments.get("path"), depth=..., limit=...)
```

From `backend/app/services/chat.py`:
```python
if looks_like_explorer_request(content):
    explorer_result = run_explorer_sub_agent(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the explorer-facing `ls` and `tree` tool contract</name>
  <read_first>
- backend/app/services/explorer_agent.py
- backend/app/services/knowledge_base.py
- backend/tests/test_prd_smoke.py
- .planning/phases/04-navigation-tools/04-CONTEXT.md
- .planning/phases/04-navigation-tools/04-RESEARCH.md
  </read_first>
  <files>backend/app/services/explorer_agent.py, backend/tests/test_prd_smoke.py</files>
  <acceptance_criteria>
- `python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_explorer_agent_navigation_tools_expose_phase_4_contracts -v` passes.
- `rg -n "\"name\": \"ls\"|\"name\": \"tree\"" backend/app/services/explorer_agent.py` confirms the navigation tools remain registered in `EXPLORER_TOOLS`.
- `rg -n "test_explorer_agent_navigation_tools_expose_phase_4_contracts" backend/tests/test_prd_smoke.py` finds the explorer-contract regression.
  </acceptance_criteria>
  <action>Adjust `backend/app/services/explorer_agent.py` so the exposed `ls` and `tree` definitions stay concise and explicit for agent use per D-11: document `/`, `/global`, and `/private` path expectations per D-02, make clear that `ls` returns structured entries per D-07, and keep `tree` depth/limit/truncation semantics obvious per D-08 and D-09. Preserve the existing Python-service dispatch to `execute_ls()` and `execute_tree()` per D-10, keep grep/glob/read/analyze_document present but untouched beyond coexistence safeguards per D-01, and add a regression in `backend/tests/test_prd_smoke.py` that proves the navigation tools stay registered and that fallback filesystem-structure requests still go through `execute_tree()` rather than bypassing navigation.</action>
  <verify>
    <automated>python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_explorer_agent_navigation_tools_expose_phase_4_contracts -v</automated>
  </verify>
  <done>The explorer registry advertises Phase 4 navigation correctly, and its direct `ls`/`tree` wiring is protected against accidental contract drift.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve chat-side routing for filesystem-style navigation requests</name>
  <read_first>
- backend/app/services/chat.py
- backend/app/services/explorer_agent.py
- backend/app/services/knowledge_base.py
- backend/tests/test_prd_smoke.py
- .planning/phases/04-navigation-tools/04-VALIDATION.md
  </read_first>
  <files>backend/app/services/chat.py, backend/tests/test_prd_smoke.py</files>
  <acceptance_criteria>
- `python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_chat_routes_navigation_requests_through_explorer_context -v` passes.
- `rg -n "looks_like_explorer_request\\(content\\)|run_explorer_sub_agent\\(" backend/app/services/chat.py` confirms the existing explorer-routing branch remains intact.
- `rg -n "test_chat_routes_navigation_requests_through_explorer_context" backend/tests/test_prd_smoke.py` finds the chat-routing regression.
  </acceptance_criteria>
  <action>Keep `stream_conversation_reply()` in `backend/app/services/chat.py` aligned with the existing knowledge-base explorer architecture per D-10: filesystem-style questions must continue routing through `looks_like_explorer_request()` into `run_explorer_sub_agent()`, explorer findings must remain part of the grounded answer context when present, and no Phase 5-7 search/read/orchestration expansion should be introduced per D-01. Add a regression in `backend/tests/test_prd_smoke.py` that patches `run_explorer_sub_agent()` for a navigation request and proves the chat flow uses explorer output instead of bypassing the explorer path or immediately falling back to unrelated tooling.</action>
  <verify>
    <automated>python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_chat_routes_navigation_requests_through_explorer_context -v</automated>
  </verify>
  <done>Filesystem-style chat requests still reach the explorer path, and the integration is regression-tested for TOOL-01 and TOOL-02 consumers.</done>
</task>

</tasks>

<verification>
- `python -m unittest backend.tests.test_prd_smoke -v`
- Confirm `backend/app/services/explorer_agent.py` still exposes only contract/documentation hardening for `ls` and `tree`, not new Phase 5-7 behavior.
</verification>

<success_criteria>
- The explorer tool registry exposes `ls` and `tree` with concise, root-aware, agent-friendly descriptions.
- Explorer dispatch still calls the hardened navigation primitives from Plan 04-01.
- Chat routing preserves filesystem-style explorer delegation and is covered by automated regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-tools/04-02-SUMMARY.md`
</output>
