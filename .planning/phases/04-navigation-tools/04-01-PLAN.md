---
phase: 04-navigation-tools
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/knowledge_base.py
  - backend/tests/test_prd_smoke.py
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
must_haves:
  truths:
    - "`ls('/')` returns only the `/global` and `/private` virtual roots."
    - "`ls('/private')` shows only the caller's visible private child folders and private-root documents."
    - "`tree(path, depth, limit)` returns hierarchical text plus `truncated` metadata without leaking hidden content."
    - "Invalid roots and missing folder paths fail with explicit navigation errors."
  artifacts:
    - path: backend/app/services/knowledge_base.py
      provides: "Stable `execute_ls()` and `execute_tree()` path, visibility, and truncation behavior"
    - path: backend/tests/test_prd_smoke.py
      provides: "Direct smoke coverage for ls/tree root, scope, error, depth, and truncation behavior"
  key_links:
    - from: backend/app/services/knowledge_base.py
      to: backend/app/services/folders.py
      via: "visible_folder_clause()/visible_document_clause()"
      pattern: "visible_(folder|document)_clause"
    - from: backend/tests/test_prd_smoke.py
      to: backend/app/services/knowledge_base.py
      via: "direct `execute_ls()`/`execute_tree()` calls under `bind_current_user_context()`"
      pattern: "execute_(ls|tree)"
---

<objective>
Stabilize the existing Python navigation service so Phase 4's `ls` and `tree` contracts are correct, explicit, and regression-tested.

Purpose: Lock TOOL-01 and TOOL-02 onto the already-shipped implementation before later explorer work builds on top of it.
Output: Hardened `execute_ls()`/`execute_tree()` behavior plus direct backend smoke coverage for root paths, scoped paths, invalid paths, depth, limit, truncation, and visibility.
</objective>

<execution_context>
@C:/Users/olivi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/olivi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-navigation-tools/04-CONTEXT.md
@.planning/phases/04-navigation-tools/04-RESEARCH.md
@.planning/phases/04-navigation-tools/04-VALIDATION.md
@backend/app/services/knowledge_base.py
@backend/app/services/folders.py
@backend/app/db/models.py
@backend/tests/test_prd_smoke.py

<interfaces>
From `backend/app/services/knowledge_base.py`:
```python
ROOT_SCOPES = ("global", "private")

def resolve_path(path: str | None, folders: list[Folder], user_id: UUID | str) -> ResolvedPath: ...
def execute_ls(db: Session, user_id: UUID | str, path: str | None = None) -> dict[str, Any]: ...
def execute_tree(
    db: Session,
    user_id: UUID | str,
    path: str | None = None,
    *,
    depth: int = 3,
    limit: int = 80,
) -> dict[str, Any]: ...
```

Current return contracts:
```python
{"path": "/", "entries": [{"kind": "folder", "name": "global", "path": "/global", "scope": "global"}, ...]}
{"path": "/private", "depth": 3, "limit": 80, "truncated": False, "output": "..."}
```

From `backend/tests/test_prd_smoke.py`:
```python
def create_folder(...): ...
def create_completed_document(...): ...

with SessionLocal() as db:
    bind_current_user_context(db, str(user_id))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock `ls()` root and scoped-path semantics</name>
  <read_first>
- backend/app/services/knowledge_base.py
- backend/app/services/folders.py
- backend/app/db/models.py
- backend/tests/test_prd_smoke.py
- .planning/phases/04-navigation-tools/04-CONTEXT.md
  </read_first>
  <files>backend/app/services/knowledge_base.py, backend/tests/test_prd_smoke.py</files>
  <acceptance_criteria>
- `python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_knowledge_base_ls_lists_virtual_roots_and_scoped_entries -v` passes.
- `rg -n "test_knowledge_base_ls_lists_virtual_roots_and_scoped_entries" backend/tests/test_prd_smoke.py` finds the dedicated TOOL-01 regression.
- `rg -n "Knowledge-base paths must start with /global or /private|Folder path not found" backend/app/services/knowledge_base.py` confirms explicit invalid-path failures remain in code.
  </acceptance_criteria>
  <action>Update `execute_ls()` in `backend/app/services/knowledge_base.py` so the Phase 4 virtual-root contract is explicit and correct: keep `/` limited to the two virtual roots per D-02 and D-06, keep `/private` showing only the caller's visible child folders plus that caller's `folder_id IS NULL` private-root documents per D-05 and D-06, and ensure `/global` never leaks private-root documents just because `folder_id` is null. Preserve the current virtual-root traversal instead of adding persisted root-folder rows per D-03, keep structured folder/document entries with machine-usable fields per D-07, keep explicit invalid-root and missing-folder errors per D-04, and extend `backend/tests/test_prd_smoke.py` with a focused `ls` regression that proves `/`, `/private`, `/global`, invalid-root, and missing-folder behavior. Do not add grep/glob/read behavior or a separate service surface per D-01 and D-10.</action>
  <verify>
    <automated>python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_knowledge_base_ls_lists_virtual_roots_and_scoped_entries -v</automated>
  </verify>
  <done>`execute_ls()` enforces the Phase 4 root/path contract and the smoke suite locks TOOL-01 behavior against future regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock `tree()` depth, truncation, and visibility behavior</name>
  <read_first>
- backend/app/services/knowledge_base.py
- backend/app/services/folders.py
- backend/app/db/models.py
- backend/tests/test_prd_smoke.py
- .planning/phases/04-navigation-tools/04-VALIDATION.md
  </read_first>
  <files>backend/app/services/knowledge_base.py, backend/tests/test_prd_smoke.py</files>
  <acceptance_criteria>
- `python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_knowledge_base_tree_respects_depth_limit_truncation_and_visibility -v` passes.
- `rg -n "test_knowledge_base_tree_respects_depth_limit_truncation_and_visibility" backend/tests/test_prd_smoke.py` finds the dedicated TOOL-02 regression.
- `rg -n "\"depth\": depth|\"limit\": limit|\"truncated\": truncated" backend/app/services/knowledge_base.py` confirms the tree metadata contract stays explicit.
  </acceptance_criteria>
  <action>Update `execute_tree()` in `backend/app/services/knowledge_base.py` so it mirrors the hardened scope rules from Task 1 and preserves the Phase 4 browsing contract: `/`, `/global`, and `/private` must render only visible content per D-05 and D-06; output must remain hierarchical text plus `path`, `depth`, `limit`, and `truncated` metadata per D-08; and `depth`/`limit` must remain first-class controls rather than best-effort hints per D-09. Extend `backend/tests/test_prd_smoke.py` with a focused tree regression that covers nested folders, private-root visibility, `/global` isolation, depth limiting, and truncation when the line limit is exceeded. Do not refactor toward database procedures or Phase 5-7 tools per D-01 and D-10.</action>
  <verify>
    <automated>python -m unittest backend.tests.test_prd_smoke.PRDSmokeTests.test_knowledge_base_tree_respects_depth_limit_truncation_and_visibility -v</automated>
  </verify>
  <done>`execute_tree()` produces agent-friendly hierarchical output with stable depth/limit/truncation semantics, and TOOL-02 is protected by direct smoke coverage.</done>
</task>

</tasks>

<verification>
- `python -m unittest backend.tests.test_prd_smoke -v`
- Re-read `backend/app/services/knowledge_base.py` and confirm the root/document filters for `/global` and `/private` are intentionally different.
</verification>

<success_criteria>
- TOOL-01 is satisfied by a tested `execute_ls()` contract rooted at `/`, `/global`, and `/private`.
- TOOL-02 is satisfied by a tested `execute_tree()` contract with explicit depth, limit, and truncation behavior.
- Direct smoke coverage exists for invalid roots, missing folders, scoped visibility, and tree truncation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-tools/04-01-SUMMARY.md`
</output>
